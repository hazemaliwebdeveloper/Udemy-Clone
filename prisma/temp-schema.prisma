generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url = "file:./dev.db"
}

enum Role {
  STUDENT
  TEACHER
}

model User {
  id String @id @default(uuid())
  role Role @default(STUDENT)
}
